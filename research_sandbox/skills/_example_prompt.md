# Example instruction skill

This file is NOT auto-loaded (underscore prefix). Rename it without the underscore
to activate it.

---

When answering questions about this dataset, always:

1. State the row count of any table you query before interpreting results.
2. If a numeric column has more than 5 % null values, flag it explicitly.
3. Prefer percentage breakdowns over raw counts when comparing segments.
4. If the user asks "why", acknowledge that the data shows correlation only —
   never claim causation without explicit evidence.
